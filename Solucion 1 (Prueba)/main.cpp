#include "Tareas/Tareas.h"

int main() {

    asignacionLista();

    imprimirTareas(lista1, count1, "Empresario");
    imprimirTareas(lista2, count2, "Manager 1");
    imprimirTareas(lista3, count3, "Manager 2");

    return 0;
}